const 
l = document.querySelector('.btnl'),
letterL = document.querySelectorAll('.l'),
i = document.querySelector('.btni'),
letterI = document.querySelectorAll('.i'),
g = document.querySelector('.btng'),
letterG = document.querySelectorAll('.g'),
h = document.querySelector('.btnh'),
letterH = document.querySelectorAll('.h'),
t = document.querySelector('.btnt'),
letterT = document.querySelectorAll('.t'),
d = document.querySelector('.btnd'),
letterD = document.querySelectorAll('.d'),
a = document.querySelector('.btna'),
letterA = document.querySelectorAll('.a'),
y = document.querySelector('.btny'),
letterY = document.querySelectorAll('.y'),
f = document.querySelector('.btnf'),
letterF = document.querySelectorAll('.f'),
s = document.querySelector('.btns'),
letterS = document.querySelectorAll('.s'),
u = document.querySelector('.btnu'),
letterU = document.querySelectorAll('.u'),
r = document.querySelector('.btnr'),
letterR = document.querySelectorAll('.r'),
b = document.querySelector('.btnb'),
letterB = document.querySelectorAll('.b'),
m = document.querySelector('.btnm'),
letterM = document.querySelectorAll('.m'),
n = document.querySelector('.btnn'),
letterN = document.querySelectorAll('.n')



l.addEventListener('click', (e)=>{
   // e.preventDefault()
   letterL.forEach(element => {
    element.textContent = 'l'
   });
})

i.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterI.forEach(element => {
     element.textContent = 'i'
    });
 })

 g.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterG.forEach(element => {
     element.textContent = 'g'
    });
 })

 h.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterH.forEach(element => {
     element.textContent = 'h'
    });
 })

 t.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterT.forEach(element => {
     element.textContent = 't'
    });
 })

 d.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterD.forEach(element => {
     element.textContent = 'd'
    });
 })

 a.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterA.forEach(element => {
     element.textContent = 'a'
    });
 })

 y.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterY.forEach(element => {
     element.textContent = 'y'
    });
 })

 f.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterF.forEach(element => {
     element.textContent = 'f'
    });
 })

 s.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterS.forEach(element => {
     element.textContent = 's'
    });
 })

 u.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterU.forEach(element => {
     element.textContent = 'u'
    });
 })

 r.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterR.forEach(element => {
     element.textContent = 'r'
    });
 })

 b.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterB.forEach(element => {
     element.textContent = 'b'
    });
 })

 m.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterM.forEach(element => {
     element.textContent = 'm'
    });
 })

 n.addEventListener('click', (e)=>{
    // e.preventDefault()
    letterN.forEach(element => {
     element.textContent = 'n'
    });
 })

